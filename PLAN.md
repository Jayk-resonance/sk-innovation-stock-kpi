# SK이노베이션 CEO KPI '주가' 과제 — 산출·검증 시스템 구축 계획

> 상태: **계획 단계** (사이트 미구현). 본 문서 승인 후 구현 착수.
> 작성일: 2026-07-27

---

## 0. 사전 검증 결과 (완료)

계획 수립 전 데이터 소스 실현 가능성을 먼저 확인했다.

| 확인 항목 | 결과 |
|---|---|
| PlayMCP `한국주식정보` 커넥터 | 정상 (`kis_configured: true`, `dart_configured: true`) |
| 9개 종목 코드 해석 | 9/9 성공 |
| 일별 OHLCV + **거래대금** 제공 | **제공됨** (`trading_value`) |
| 과거(2025년) 데이터 소급 조회 | 가능 (`start_date`/`end_date` 지정) |
| 1회 조회 한도 | 100건 (2개월 ≒ 41거래일 → 1콜로 충족) |

**핵심 소득:** 응답에 `trading_value`(거래대금)가 포함되어 체결 기준 VWAP
(**Σ거래대금 ÷ Σ거래량**)을 정확히 계산할 수 있다. 사내 기존 방식인
`종가 × 거래량` 가중평균과 **두 방식을 모두 산출·대조할 수 있다는 뜻**이며,
그 검증 결과는 §2에 있다.

### 종목 마스터

| 그룹 | 종목 | 코드 | 시장 |
|---|---|---|---|
| 본사 | SK이노베이션 | 096770 | KOSPI |
| 에/화 (60%) | LG화학 | 051910 | KOSPI |
| 에/화 | S-Oil | 010950 | KOSPI |
| 에/화 | 롯데케미칼 | 011170 | KOSPI |
| 에/화 | 포스코인터내셔널 | 047050 | KOSPI |
| 배/소 (40%) | LG에너지솔루션 | 373220 | KOSPI |
| 배/소 | 삼성SDI | 006400 | KOSPI |
| 배/소 | 에코프로비엠 | 247540 | **KOSDAQ GLOBAL** |
| 배/소 | 포스코퓨처엠 | 003670 | KOSPI |

> 에코프로비엠만 KOSDAQ이다. 휴장일 캘린더는 KOSPI와 동일하나
> 데이터 조회 파라미터가 다를 수 있어 수집 단계에서 별도 검증 대상으로 둔다.

---

## 1. 확정된 산출 로직

### 1.1 거래량 보정 주가

**산식은 2가지 Case를 모두 구현한다** (§2 검증 결과 반영).

```
Case A (체결 기준) : VWAP = Σ 거래대금 / Σ 거래량
Case B (종가 기준) : VWAP = Σ (종가 × 거래량) / Σ 거래량   ← 사내 기존 방식
```

| 평가 시점 | 거래량 보정 주가 정의 |
|---|---|
| 잠정평가 (4월·7월·10월) | 2개월 VWAP |
| 최종평가 (11월) | (2개월 VWAP + 1개월 VWAP + 1주 VWAP) / 3 |

`config/kpi_rules.yaml`의 `vwap_method: A | B`로 전환하며, 사이트에서는
두 Case를 항상 병렬 표기한다. 기본값은 사내 관행인 **B**로 두되,
어느 쪽을 공식으로 할지는 §7-2에서 확정한다.

### 1.2 평가주가

```
SK증감율    = SK_VWAP(평가일)   / SK_VWAP(기준일)   − 1
Peer_i증감율 = Peer_i_VWAP(평가일) / Peer_i_VWAP(기준일) − 1

에화평균 = mean(LG화학, S-Oil, 롯데케미칼, 포스코인터내셔널)
배소평균 = mean(LG엔솔, 삼성SDI, 에코프로비엠, 포스코퓨처엠)

Peer증감율 = 0.6 × 에화평균 + 0.4 × 배소평균

x (상대 증감율) = SK증감율 − Peer증감율        ← 확정

평가주가 = SK 거래량보정주가(평가일) × 1 / (1 − x)
```

**해석 확정 근거:** SK가 Peer 대비 20%p 부진하면 `x = −0.20` →
`1/1.2 = 0.833배`로 할인되어 감점된다. 상대평가 취지와 방향이 일치한다.

**기준 일치 원칙:** `x` 산출 시 SK와 Peer 8개사는 **반드시 동일한 VWAP 정의**
(동일 윈도우·동일 기준일)를 사용한다. 코드 레벨에서 SK와 Peer가 서로 다른
`window_spec`을 참조하면 예외를 발생시켜 구조적으로 차단한다.

### 1.3 점수 환산

기준선(40점) = **113,109원 고정** (연초 안내값)

| 앵커 | 평가주가 | 점수 |
|---|---|---|
| −15% | 96,143원 | 0점 |
| 기준 | 113,109원 | 40점 |
| +15% | 130,075원 | 100점 |

```
평가주가 ≤ 113,109 :  점수 = 40 × (평가주가 − 96,143) / (113,109 − 96,143)
평가주가 >  113,109 :  점수 = 40 + 60 × (평가주가 − 113,109) / (130,075 − 113,109)
```

> 상·하한 클리핑(0점 미만 / 100점 초과) 처리는 규정 확인 필요 →
> §7 미결 항목. 기본값은 `clip(0, 100)`으로 두되 원값도 함께 표기한다.

### 1.4 기준선 3개 버전

| 버전 | 기준값 | 성격 | 용도 |
|---|---|---|---|
| **V3 (공식)** | 113,109원 (고정) | 외부 안내값 | **주 지표.** 실제 KPI 점수 |
| V1 | 2개월 VWAP 재현값 | KIS 자체 산출 | 참고. 산출근거 추적 |
| V2 | 2M·1M·1W 산술평균 재현값 | KIS 자체 산출 | **최종평가 예상 기준선** (§3) |

**세 버전은 모두 시뮬레이션 가능하며, 성격이 다르다는 점이 중요하다.**
V1·V2는 기준선과 평가일 주가가 **같은 출처(KIS)**에서 나오므로 §3의
2.5% 스케일 불일치가 상쇄된다 — 즉 **내부적으로 일관된 상대 성과**를 보여준다.
V3는 공식 점수지만 출처가 섞여 있다. 셋을 나란히 놓으면
"공식 점수는 이것이고, 데이터 일관성만 보면 이렇다"를 동시에 말할 수 있다.

---

## 2. 검증 ①: 거래량가중평균 산식 A vs B (완료)

사내 기존 방식(B)이 체결 기준(A)과 실제로 다른지 9개 종목 전량 검증했다.

### 결론: 다르다. 단, 점수 영향은 1점 미만이다.

**종목별 2개월 VWAP (2025-11-03 ~ 12-30)**

| 종목 | A: Σ대금/Σ량 | B: Σ(종가×량)/Σ량 | 차이 |
|---|---:|---:|---:|
| SK이노베이션 | 116,002 | 115,800 | −0.17% |
| LG화학 | 379,157 | 378,985 | −0.05% |
| S-Oil | 82,067 | 82,019 | −0.06% |
| 롯데케미칼 | 76,801 | 77,023 | **+0.29%** |
| 포스코인터내셔널 | 53,141 | 53,105 | −0.07% |
| LG에너지솔루션 | 431,317 | 430,880 | −0.10% |
| 삼성SDI | 306,534 | 306,455 | −0.03% |
| 에코프로비엠 | 161,751 | 161,554 | −0.12% |
| 포스코퓨처엠 | 207,620 | 207,425 | −0.09% |

**일 단위로는 훨씬 크게 벌어진다.** 종가와 당일 실제 VWAP의 괴리는
평균 ±0.1% 수준이나 최대 **±3.6%**(롯데케미칼)까지 발생하며,
9종목 41거래일 총 369건 중 두 값이 일치한 날은 **단 1건**이다.
즉 "매일 다른가"의 답은 **명백히 다르다**. 다만 2개월을 누적하면
일별 괴리가 상쇄되어 종목 레벨 차이는 ±0.3% 이내로 수렴한다.

### 점수 영향 (실측)

기준일 2025-12-30, 평가일 2026-07-27, 기준선 113,109원 고정.

| 구분 | 항목 | 방식 A | 방식 B | 차이 |
|---|---|---:|---:|---:|
| **잠정** (2M) | 상대 증감율 x | −11.93% | −11.93% | −0.005%p |
| | 평가주가 | 99,827원 | 99,611원 | −216원 |
| | **점수** | **8.69점** | **8.18점** | **−0.51점** |
| **최종** (2M·1M·1W) | 상대 증감율 x | +1.79% | +1.68% | −0.116%p |
| | 평가주가 | 119,339원 | 119,094원 | −245원 |
| | **점수** | **62.03점** | **61.16점** | **−0.87점** |

**왜 이렇게 작은가 —** 산식 차이는 SK와 Peer 양쪽에 같은 방향으로 작용하고,
`x = SK증감율 − Peer증감율`은 비율의 차이라서 편향이 대부분 상쇄된다
(잠정평가 x 차이 0.005%p). 남는 영향은 앞항 `SK 거래량보정주가` 레벨
차이뿐이라 0.5~0.9점에 그친다.

**그럼에도 두 Case를 모두 만든다.** 0.5~0.9점은 등급 경계에서 뒤집힐 수 있는
크기이고, 무엇보다 **어느 산식이 공식인지 문서로 확정되어야** 분기 평가에서
방어가 된다. 두 값을 함께 보여주면 그 논의 자체가 불필요해진다.

### 부수 발견: 잠정 vs 최종 방식 차이가 훨씬 크다

같은 날 같은 데이터인데 **잠정 방식 8.7점 / 최종 방식 62.0점**으로
**53점** 벌어진다. 최근 SK 주가 반등이 1주·1개월 윈도우에는 반영되고
2개월 윈도우에는 희석되기 때문이다. 산식 A/B 논쟁(1점)보다
**윈도우 선택(53점)이 비교할 수 없이 중요하다.**
27년 과제 설계 시 최우선 검토 대상이며, 사이트 탭 3의 핵심 화면으로 둔다.

### 2026년 잠정평가 실적 (2단계 백필 완료 후 확정)

| 평가 | 평가일 | SK 증감율 | Peer 증감율 | x | 평가주가 | 원값 | **점수** |
|---|---|---:|---:|---:|---:|---:|---:|
| 1분기 | 2026-04-16 | +4.29% | +18.75% | −14.46% | 106,219원 | +23.76 | **23.76점** |
| 상반기 | 2026-07-15 | −6.96% | +11.07% | −18.03% | 91,896원 | −10.01 | **0.00점** |

상반기는 기준선 3버전(V3/V1/V2) 모두 0점이다. **어느 기준선을 써도 결론이 같다.**
1분기는 V3 23.76 / V1 16.31 / V2 30.84 로 기준선에 따라 14점 벌어진다.

두 분기 모두 SK 자체는 Peer 대비 크게 뒤졌다. 1분기 −14.5%p, 상반기 −18.0%p 로
**격차가 벌어지는 추세**다. 특히 배/소 그룹이 두 시점 모두 강세였다.

### 참고: 평가일 민감도 (7/27 기준 산출과의 비교)

평가일이 7/27이 아니라 **7/15**로 확정되면서 위 예시 수치가 크게 바뀐다.

| 방식 | 상대 증감율 x | 평가주가 | 원값 | **클리핑 후** |
|---|---:|---:|---:|---:|
| 잠정 (2M) · 산식 A | −17.31% | 92,087원 | −9.56점 | **0.00점** |
| 잠정 (2M) · 산식 B | −17.29% | 92,026원 | −9.71점 | **0.00점** |
| 최종 (2M·1M·1W) · A | −9.07% | 98,092원 | +4.60점 | 4.60점 |
| 최종 (2M·1M·1W) · B | −9.10% | 97,705원 | +3.68점 | 3.68점 |

**상반기 잠정평가는 0점(하한)이다.** 평가주가 92,026원이 0점 기준선
96,143원보다 4,117원(−4.3%) 낮아 하한에 걸렸다. 클리핑을 적용하지 않았다면
−9.7점이므로, §7.1-4의 클리핑 결정이 곧바로 유효한 상황이다.

**평가일 하루 차이가 8점을 만든다.** 같은 잠정 방식으로 7/27에 계산하면
8.2점인데 7/15 기준은 0점이다. 7/16~7/27 사이 SK 주가가 11만원대에서
13만원대로 반등했으나 **평가일 이후라 반영되지 않는다.**
평가일을 코드가 추정하면 안 되는 이유가 여기 있다(§7.1-5).

---

## 3. ⚠️ 미해결 리스크: 기준값 재현 불일치

2025년말 SK이노베이션 VWAP을 KIS 데이터로 재현했으나 안내값과 일치하지 않는다.

| 산출 방식 | 재현값 | 113,109원 대비 |
|---|---|---|
| 2개월 VWAP (11/01~12/30) | 116,002원 | **+2.9%** |
| 2M·1M·1W 산술평균 | 110,161원 | **−2.6%** |
| 2개월 일별VWAP 단순평균 | 114,925원 | +1.6% |
| 2개월 종가 단순평균 | 114,768원 | +1.5% |

**어떤 정의로도 113,109원이 재현되지 않는다.**

### 왜 문제인가

평가주가의 앞항 `SK 거래량보정주가(평가일)`는 KIS 데이터로 계산되는데,
비교 기준선 113,109원은 다른 출처값이다. 두 값의 **스케일이 약 2.5% 어긋난 채**
직접 비교되면 그 편차가 그대로 점수 편향으로 들어간다.
### 편향의 실제 크기 (1단계 엔진으로 측정 완료)

가장 명확한 진단은 이것이다 — **기준일(2025-12-30)을 그대로 넣으면 몇 점이 나오는가.**
정의상 40점이 나와야 한다. 실제로는:

| 평가 방식 | SK 보정주가(기준일) | 113,109원 대비 | V3 점수 | **편향** |
|---|---:|---:|---:|---:|
| 잠정 (2M) | 116,576원 | **+3.07%** | 52.26점 | **+12.26점** |
| 최종 (2M·1M·1W) | 109,995원 | **−2.75%** | 32.66점 | **−7.34점** |

자기일관 기준선(V1·V2)은 같은 조건에서 **정확히 40.00점**이 나온다(테스트로 고정).

**편향의 부호가 평가 방식에 따라 뒤집힌다는 점이 결정적이다.**
113,109원은 2개월 VWAP(116,576)과 3종 평균(109,995) **사이에** 있다.
조정계수 같은 단일 스케일 문제였다면 부호가 뒤집힐 수 없다.
→ **윈도우 정의 차이가 원인일 가능성이 크게 높아졌다.**

### 유력 원인 가설

1. ~~무수정주가·권리락 조정~~ — **DART 조회로 반증됨.**
   SK이노베이션 2025년 발행공시(`C`)에는 채무증권 발행뿐이고
   **유상증자가 없다.** 권리락 조정계수 가설은 유력도가 크게 떨어졌다.
   (다만 자사주 소각 등 다른 유형은 §5.2 절차로 추가 확인 필요)
2. **기준 윈도우 정의 상이 (가장 유력)** — 113,109원이 2M VWAP 과 3종 평균
   사이에 위치하며 편향 부호가 방식에 따라 뒤집힌다는 사실이 이를 강하게 시사한다.
   단일 조정계수로는 설명되지 않는 패턴이다.
3. 산출 출처 상이 (KRX 공식 거래량가중평균 vs KIS 집계)

### 해소 경로 확정: 최종평가 시 자연 해소

**최종평가(11월)에 목표와 실적을 모두 산술평균 기준으로 업데이트한다**는 방침이
확인되었다(2026-07-27). 이 시점에 기준선이 3종 평균 기준으로 재산정되므로
앵커와 실적의 산출 기준이 일치하게 되고, **본 불일치는 소멸한다.**

따라서 산출근거 역추적은 하지 않는다. 정리하면:

| 기간 | 기준선 | 상태 |
|---|---|---|
| 잠정평가 (4·7·10월) | 113,109원 (2M 기준) | **+12.3점 편향을 안고 간다.** 알려진 값이므로 배지로 명시 |
| 최종평가 (11월~) | 산술평균 기준으로 재산정 | 자기일관. **V2가 이 상태의 미리보기다** |

→ V2(109,995원)를 단순 참고지표가 아니라 **최종평가 예상 기준선**으로 격상해
   사이트에 표기한다. 잠정 점수와 최종 예상 점수를 나란히 보는 것이 실무상 유용하다.

### 대응 (구현 시 반영)

- `config/calibration.yaml`에 `calibration_factor` 항목을 둔다 (기본 1.0).
- 사이트에 **"기준값 검증 미완료"** 배지를 상시 노출하고,
  보정계수 0.9751 적용 시 점수를 나란히 표시한다.
- **§5 분기 검증 프로세스의 1순위 해소 과제로 등록한다.**
- 산출근거 역추적은 **하지 않는다** (위 방침에 따라 불필요).

---

## 4. 시스템 구조

### 4.1 설계 원칙

**수집 / 계산 / 배포를 완전히 분리한다.**

MCP 커넥터는 Claude 세션에서만 호출 가능하므로 일반 cron·GitHub Actions에서
직접 부를 수 없다. 따라서 수집만 Claude가 담당하고, **계산은 원자료 JSON을
입력으로 받는 순수 Python**으로 만든다. 이 분리의 효과는 크다.

- 계산 로직을 오프라인에서 단위테스트할 수 있다
- 저장된 원자료로 **언제든 동일 결과를 재현**할 수 있다 (→ 검증의 전제)
- 데이터 소스를 교체해도 계산 코드는 그대로다

### 4.2 폴더 구조

```
stock_kpi/
├── PLAN.md                     # 본 문서
├── config/
│   ├── universe.yaml           # 9종목, 그룹, 가중치(60/40)
│   ├── kpi_rules.yaml          # 윈도우 정의, 점수 스케일, 평가일정
│   └── calibration.yaml        # 113,109 기준선, 보정계수, 수동 오버라이드
├── core/                       # ← 순수 함수. MCP·네트워크 의존성 없음
│   ├── calendar.py             # 거래일 캘린더, 윈도우 경계 산출
│   ├── vwap.py                 # VWAP 계산
│   ├── evaluate.py             # 평가주가 · Peer증감율 · 점수
│   ├── scenarios.py            # 시뮬레이션 엔진
│   └── schema.py               # 스키마 정의 및 검증
├── pipeline/
│   ├── COLLECT.md              # Claude 수집 실행 지침 (MCP 호출 규격)
│   ├── ingest.py               # raw JSON → 정규화 CSV
│   ├── build_site.py           # 계산 → docs/data/*.json
│   └── verify.py               # 검증 루틴
├── data/
│   ├── raw/YYYY-MM-DD.json     # 원자료. append-only, 절대 수정 금지
│   ├── normalized/prices.csv   # 누적 시계열
│   └── snapshots/              # 분기 평가 시점 동결본
├── docs/                       # GitHub Pages 루트
│   ├── index.html
│   ├── assets/{app.js,style.css}
│   └── data/{latest.json,history.json,scenarios.json}
├── tests/
│   ├── test_vwap.py
│   ├── test_evaluate.py        # 기준일 x=0 → 평가주가=기준값 등 항등식 검증
│   └── fixtures/
└── reports/                    # 분기 검증 보고서 (산출근거 패키지)
```

**`data/raw/`는 불변(immutable)으로 취급한다.** 재계산은 항상 원자료에서
다시 수행한다. 이것이 분기 검증에서 "그때 그 숫자"를 증명하는 근거가 된다.

### 4.3 Agent 구조

`.claude/agents/` 에 4개 전용 에이전트를 정의한다.

| Agent | 역할 | 도구 | 실행 |
|---|---|---|---|
| `kpi-collector` | MCP로 9종목 일봉 수집 → `data/raw/` 저장. 결측·거래정지 1차 판정 | PlayMCP, Write, Bash | 매일 |
| `kpi-calculator` | 원자료 → 평가주가·점수 산출. 3개 기준선 × 2개 방식 전량 계산 | Read, Bash, Write | 매일 |
| `kpi-verifier` | 교차검증·이상탐지·재현성 확인. **분기 평가 시 필수 게이트** | Read, Bash, WebSearch, PlayMCP | 매일(경량) / 분기(정밀) |
| `kpi-publisher` | 사이트 데이터 갱신, commit & push, 이상 시 알림 | Read, Write, Bash | 매일 |

> `kpi-simulator`(27년 과제 설계용)는 상시 실행이 아닌 **온디맨드**로 둔다.
> 매일 돌릴 이유가 없고, 탐색적 분석이라 대화형이 더 적합하다.

---

## 5. 프로세스

### 5.1 매일 프로세스 (장 마감 후 KST 16:00)

Claude **Routine**으로 발화한다 (`0 7 * * 1-5` UTC = 평일 16:00 KST).

```
1. 휴장일 판정          → 휴장이면 즉시 종료 (no-op 커밋 없음)
2. kpi-collector        → 9종목 일봉 조회, data/raw/YYYY-MM-DD.json 저장
3. QC 게이트 ①          → 9종목 전량 수신? 당일 날짜 일치? 거래량 0?
                          전일 대비 ±30% 초과? → 실패 시 중단 + 알림
4. kpi-calculator       → 평가주가·점수 산출 (기준선 3버전 × 방식 2종)
5. QC 게이트 ②          → 전일 대비 점수 급변(±5점) 시 사유 자동 진단
6. kpi-verifier (경량)  → 원자료 재계산 일치 확인, 시계열 연속성 검사
7. kpi-publisher        → docs/data/*.json 갱신, commit & push
8. 알림                 → 점수 ±3점 변동 또는 QC 실패 시에만 발송
```

**설계 의도 — 조용한 파이프라인:** 정상일에는 알림을 보내지 않는다.
매일 오는 알림은 곧 읽히지 않는 알림이 된다. 이상 시에만 울려야 신호가 산다.

**실패 처리:** 어느 단계든 실패하면 `docs/data`를 갱신하지 않는다.
사이트는 마지막 정상 데이터를 유지하고 "N일 전 기준" 배지를 노출한다.
**깨진 데이터를 올리는 것보다 낡은 데이터를 유지하는 편이 안전하다.**

### 5.2 발행주식수 변동 이벤트 감지 (유상증자·자사주 소각 등)

Peer사에 유상증자·무상증자·액면분할·자사주 소각이 발생하면 주가가
기업가치와 무관하게 점프한다. KIS는 **무수정주가**(`is_adjusted: false`)를
주므로 이를 잡지 못하면 Peer 증감율이 통째로 왜곡된다.

**1차 감지 — 상장주식수 역산 (자동, 매일)**

`stock_get_quote`는 상장주식수를 직접 주지 않지만 시가총액을 준다.

```
상장주식수 = 시가총액 ÷ 주가
```

SK이노베이션 실측: 시총 197,285억원 ÷ 116,700원 = **169,053,128주**.
시가총액이 억원 단위로 반올림되어도 주식수 정밀도는 **±0.0003%**이므로,
0.1% 이상의 주식수 변동은 **여유 395배**로 확실히 감지된다.

→ 9종목 전부 매일 역산해 저장하고, **전일 대비 ±0.1% 초과 시 이벤트로 판정**한다.
숫자 신호라서 공시 제목을 파싱할 필요가 없고 누락되지 않는다. 이것이 주 감지기다.

**2차 확인 — DART 공시 조회 (자동, 이벤트 발생 시)**

1차에서 걸리면 `dart_search_filings`로 사유를 확인한다.

| 유형 코드 | 대상 | 잡히는 이벤트 |
|---|---|---|
| `B` 주요사항보고 | 유상·무상증자, 자사주 취득/소각 결정 | 사전 신호 |
| `C` 발행공시 | 증권신고서, 증권발행실적보고서 | 확정 신호 |
| `I` 거래소공시 | 주식분할·병합, 주식수 변동 | 확정 신호 |

> 실측: SK이노베이션 2025년 `C` 조회 결과 **채무증권 발행뿐이며 유상증자는 없었다.**
> 이는 §3의 "권리락 조정계수" 가설을 약화시킨다 — 113,109원 차이의 원인을
> 다른 곳(윈도우 경계 또는 산출 출처)에서 찾아야 한다는 뜻이다.

**3차 대응 — 조정계수 적용**

이벤트 확정 시 `config/calibration.yaml`의 `corporate_actions`에
`{종목, 기준일, 조정계수}`를 등록하고, 기준일 이전 주가에 소급 적용한다.
**자동 적용하지 않는다.** 계수 산정에 판단이 들어가므로 사람이 승인한 뒤
반영하고, 그 사이에는 사이트에 해당 종목 경고 배지를 띄운다.

### 5.3 분기 평가 검증 프로세스 (4월·7월·10월·11월)

일상 파이프라인과 **완전히 분리된 별도 절차**로 운영한다.

#### T−5영업일 · 데이터 완전성 감사
- 평가 윈도우 전 구간의 거래일 캘린더 대조 → **결측 0건** 확인
- 9종목 전 구간 거래대금·거래량 유효성 (0·음수·null 없음)
- 권리락/액면분할/거래정지 이벤트 스캔 (DART 공시 조회)
- 산출: `reports/{평가시점}/completeness.md`

#### T−3영업일 · 독립 교차검증
- **2차 데이터 소스로 전량 재계산** (KRX 공식자료 또는 `pykrx`)
- 종목별 VWAP 허용 오차 **±0.1%** 초과 시 개별 소명
- 계산 로직 독립 재구현본(스프레드시트)과 대조 — *구현 버그와 사양 오해를 분리하기 위함*
- 산출: `reports/{평가시점}/cross_check.md`

#### T−1영업일 · 사양 적합성 검토
- 윈도우 경계일이 규정과 일치하는가 (시작·종료 포함 여부)
- 가중치 60/40, Peer 8사 구성이 당해 확정본과 일치하는가
- 기준선 113,109원 및 보정계수 적용 상태
- **§3 미해결 리스크의 현재 상태 명시**
- 4-eyes 체크리스트 서명

#### T일 · 동결 및 산출근거 패키지
- `data/snapshots/{평가시점}/` 에 원자료 전량 동결
- **git tag** 발행 (`eval-2026-H1` 등) → 이후 코드 변경과 무관하게 재현 가능
- 산출근거 패키지 생성:
  - 종목별·일별 원자료 CSV (거래대금·거래량·종가)
  - VWAP 중간계산값 전량
  - 증감율 → Peer증감율 → 평가주가 → 점수 전 단계 추적표
  - 사용 코드 커밋 해시 + 실행 로그
- **재현성 테스트:** 동일 커밋·동일 원자료로 재실행 시 **bit-identical** 확인

> 검증의 본질은 "숫자가 맞다"가 아니라 **"이 숫자가 어떻게 나왔는지 제3자가
> 추적할 수 있다"**는 것이다. 산출근거 패키지가 그 역할을 한다.

---

## 6. 웹사이트 구성

배포: **정적 HTML + GitHub Pages** (`docs/`), 데이터는 JSON 분리.

**디자인 기준:** 기존 SK이노베이션 계열 뉴스 아카이브
(<https://jayk-resonance.github.io/Claude-web/>)의 톤·레이아웃·색상을 따른다.
사내에서 이미 통용되는 룩앤필을 재사용하면 별도 학습 없이 읽힌다.
해당 사이트는 현재 세션에 클론되지 않은 브랜치에 있으므로,
**4단계(사이트 착수) 시점에 원본을 확보한 뒤 스타일을 추출한다.**

### 요청하신 탭

| # | 탭 | 내용 |
|---|---|---|
| 1 | **주가 현황** | SK이노 + Peer 8사 종가·등락률, 기간별 차트, 그룹별 상대성과 |
| 2 | **오늘의 점수** | 평가주가·점수 대형 표시, 기준선 3버전 병렬, 다음 평가일 D-day |
| 3 | **Case 시뮬레이션** | 거래량보정주가 방식별 점수 매트릭스 — 윈도우(2M / 2M·1M·1W) × 산식(A / B) × 기준선(V1 / V2 / V3) = 12개 조합 |
| 4 | **27년 과제 설계** | 파라미터 전면 개방 시뮬레이터 (아래) |

**탭 4 조정 파라미터:** 거래량가중 적용 여부(단순종가 평균 대비) · **VWAP 산식 A/B** ·
산술평균 적용 여부 · 윈도우 조합(2M/1M/1W 외 3M/6M/1D 등 자유 조합) ·
에화:배소 가중치 슬라이더 · Peer 종목 개별 포함/제외 · 점수 스케일(±15% 조정) ·
공식 해석 토글(상대 vs 절대)

> §2 부수 발견대로 **윈도우 선택이 점수를 53점까지 흔든다.** 탭 4에서
> 가장 눈에 띄는 위치에 윈도우 조합 컨트롤을 두고, 조합별 점수를
> 히트맵으로 즉시 비교할 수 있게 한다.

### 확정 추가 기능 (전체 반영)

1. **목표 역산 계산기** — "100점 받으려면 SK 주가가 얼마여야 하는가"를 역산.
   Peer 현재가 고정 조건에서 필요 주가를 직접 제시한다. 실무에서 가장 자주
   묻는 질문이라 판단된다.
2. **Peer 기여도 분해 (Waterfall)** — 내 점수를 어느 Peer가 얼마나 깎았는지
   종목별 기여도를 분해. "LG엔솔 급등으로 −4.2점" 식으로 원인이 보인다.
3. **민감도 테이블** — SK ±1% / Peer그룹 ±1% 변동 시 점수 변화량.
   보고 시 "지금 1%가 몇 점인가"를 즉답할 수 있다.
4. **잔여기간 경로 시나리오** — 평가일까지 상승/보합/하락 3경로별 예상 점수 밴드.
   VWAP은 과거 누적이라 잔여 변동 여지가 점차 줄어드는데, 이 **"고착도"**를
   시각화하면 언제부터 만회가 어려운지 판단할 수 있다.
5. **점수 시계열 차트** — 연초 이후 일별 점수 추이 + 4개 평가시점 마커.
6. **데이터 신뢰도 배지** — 결측·무수정주가·기준값 미검증 상태를 상시 노출.
   §3 리스크가 해소되기 전까지 특히 중요하다.
7. **보고용 내보내기** — 현재 화면을 CSV/PNG로 저장. 주간보고에 바로 붙일 수 있게.
8. **변경 이력** — 설정·기준값 변경 시 누가·언제·무엇을 바꿨는지 git 기반 기록.

위 8개는 모두 구현 범위에 포함한다. 1·2·3번은 탭 2(오늘의 점수)에 붙이고,
4·5번은 탭 3, 6·7·8번은 전 탭 공통 요소로 배치한다.

---

## 7. 확정 사항 및 남은 확인 1건

### 7.1 확정 (2026-07-27 의사결정 완료)

| # | 항목 | 확정 내용 |
|---|---|---|
| 1 | 113,109원 기준선 | **보정 없이 그대로 사용.** 재현값(V1/V2)은 참고지표로 병기 |
| 2 | VWAP 산식 A/B | **둘 다 산출.** 사이트에서 항상 병렬 표기 |
| 4 | 점수 클리핑 | **`clip(0, 100)` 적용.** 원값도 함께 표기 |
| 5 | 평가일 | **SK주식회사(모회사) 안내값을 그대로 입력.** 2026년: 1분기 4/16, 상반기 7/15. 이후 일정은 안내 수신 시 config 갱신 |
| 6 | "1주" 정의 | **달력 기준 7일 전부터** (평가일 −6일 ~ 평가일). 거래일 수는 휴장에 따라 4~5일 변동 |
| 7 | Peer 거래정지 | 해당일 제외, 로그 기록 |

**5번의 구조적 의미:** 평가일이 고정 규칙이 아니라 **외부에서 통보되는 값**이므로,
`config/kpi_rules.yaml`의 `eval_dates` 리스트에 수기 입력하는 방식으로 둔다.
코드가 평가일을 추정하지 않게 하는 것이 핵심이다 — 추정하면 반드시 틀린다.
안내 미수신 시점에는 **"평가일 미확정"** 상태를 사이트에 명시하고,
잠정 계산은 직전 안내일 기준으로만 표시한다.

### 7.2 윈도우 경계 — **(가) 확정**

평가일 **2026-07-15** 기준으로 2개월 윈도우가 정확히 어느 날부터 어느 날까지인가.
아래 3가지가 모두 "2개월"로 불릴 수 있고, 어느 것이냐에 따라 값이 달라진다.

| 안 | 윈도우 | 거래일 수 |
|---|---|---|
| **(가)** 평가일 포함, 달력 2개월 | 2026-05-16 ~ **07-15** | 41일 |
| (나) 평가일 **직전일**까지 | 2026-05-15 ~ 07-14 | 41일 |
| (다) 평가일 **직전 거래일**까지 | 2026-05-15 ~ 07-14 | 41일 |

핵심 질문은 하나다 — **평가일 당일 주가가 계산에 들어가는가.**
7/15에 평가한다면 그날 종가는 장 마감 후에야 나오므로, 실무적으로는
(나)/(다)처럼 **전일까지**로 끊는 경우가 많다. 반면 규정에 "평가일까지"라고
써 있으면 (가)다.

> **(가)로 확정** (2026-07-27). `core/calendar.py` 에 구현되고 테스트로 고정되었다.
> 변경이 필요하면 `window_bounds()` 한 곳만 고치면 전체에 반영된다.

---

## 8. 구현 순서

```
0. §7.2 윈도우 경계 확인                     → 검증: 규정 원문 대조
1. config + core 계산 엔진 + 단위테스트     → 검증: 기준일 x=0 시 평가주가=기준값
2. 수집 파이프라인 + 2025년 이후 이력 백필   → 검증: 결측 0, 9종목 전량
3. 검증 루틴 + 2차 소스 교차검증            → 검증: VWAP 오차 ±0.1% 이내
4. 사이트 (탭 1~2)                          → 검증: 수기 계산과 일치
5. 사이트 (탭 3~4) + 추가 기능              → 검증: 파라미터 원복 시 탭2와 동일
6. Routine 등록 + 무인 운영 개시            → 검증: 3영업일 연속 무개입 성공
```

각 단계는 검증 조건 충족 후 다음으로 넘어간다.
**계산 엔진(1단계)이 가장 중요하다.** 여기가 틀리면 나머지가 전부 무의미하다.

---

## 부록. 데이터 커버리지 (백필 완료)

`data/normalized/prices.csv` — 9종목 × 197거래일 = **1,773행**, **2025-10-01 ~ 2026-07-27**.

| 검사 | 결과 |
|---|---|
| 수집 공백 | **0건** |
| 종목별 결측 | **0건** (9/9 전 거래일) |
| 월봉 교차검증 | **10/10개월 원 단위 일치** (VWAP 오차 0.00000000%) |

남은 경고 2건은 모두 정상 휴장이다 — 2025 추석(10/03~10/09), 2026 설(02/14~02/18).

### 백필 과정에서 드러난 임계값 불일치

`verify._contiguous_runs` 가 5일 초과 공백에서 수집 구간을 쪼개고 있었는데,
2026년 설 연휴가 6일이라 **완전히 수집한 2월이 '검증불가'로 잘못 분류됐다.**
`qc.GAP_ERROR_DAYS`(10일)로 통일했다. 같은 문제가 세 곳에 있었다.

| 위치 | 증상 |
|---|---|
| `verify._contiguous_runs` | 설 연휴에 구간이 쪼개져 2월 검증 불가 |
| `qc.check_jumps` | 하드코딩 5일 |
| `build_site.build_history` | 추석 연휴에서 차트 선이 끊김 |

**임계값을 여러 곳에 흩어 두면 반드시 어긋난다.** 상수 하나로 모았다.

### 상장주식수

`stock_get_quote` 는 시점 조회만 되고 과거 시가총액을 주지 않으므로
`상장주식수 = 시가총액 ÷ 주가` 역산을 **소급 적용할 수 없다.**
수집 개시일부터 축적된다 — 현재 0행. 그 전까지 발행주식수 변동 감지는
급변 경보(±30%)와 DART 발행공시 조회로 대신한다(§5.2).

---

## 부록 B. 3단계 검증 결과

### B.1 2차 소스 — 완전 독립 소스는 확보 불가

`pykrx` 는 KRX 계정을 요구하고 네이버 금융은 프록시가 차단한다.
대신 **KIS 월봉(`period="M"`)의 서버측 집계**를 대조 기준으로 쓴다.
우리가 일봉을 모아 만든 합계와 원 단위로 비교되므로,
수집 누락·중복·전사 오류를 잡아낸다.

**한계는 분명히 해둔다.** 이 검사가 보증하는 것은
"우리가 KIS 데이터를 온전히 옮겼다"까지다. KIS 자체가 틀린 경우는 잡지 못한다.

### B.2 대조 결과

| 월 | 판정 |
|---|---|
| 2025-11, 2026-03, 2026-06, 2026-07 | ✅ 9/9 원 단위 일치 (VWAP 오차 0.00000000%) |
| 2025-10, 2025-12, 2026-02, 2026-04, 2026-05 | ⊘ 검증불가 — 달 일부만 수집 |

**방법론상의 요건이 드러났다.** 월봉은 그 달 **전체**의 집계이므로,
달의 일부만 수집하면 정상적인 부분 수집인지 진짜 결측인지 구분할 수 없다.
→ `COLLECT.md` 에 **백필은 달 경계에 맞춘다**는 규칙을 추가했다.
평가 윈도우가 2/17에 시작해도 2월을 통째로 받아야 그 달이 검증된다.

허용오차 ±0.1% 기준은 검증 가능한 구간에서 **0.00000000%** 로 통과했다.

### B.3 ⚠️ 에코프로비엠 지분증권 발행 — 감시 필요

9종목의 2025-10-01~2026-07-27 발행공시를 전수 조회한 결과,
**지분증권(주식) 신고서를 낸 곳은 에코프로비엠 하나뿐이다.** 나머지 8사는 전부 채무증권이다.

- 2026-07-24 `[기재정정]증권신고서(지분증권)`

**상반기 평가(7/15)에는 영향이 없다** — 평가일 이후 제출이다.
그러나 권리락이 발생하면 주가가 기업가치와 무관하게 하락한다.
에코프로비엠은 배/소 그룹(가중치 40%)의 1/4이므로 **실질 10% 비중**으로
Peer 증감율을 끌어내리고, 그만큼 상대 증감율 x 가 올라가
**SK 점수가 인위적으로 상승한다.**

→ `config/calibration.yaml` 의 `watchlist` 에 등록했다.
**10월 잠정평가·11월 최종평가 전에 권리락일과 조정계수를 확정해
`corporate_actions` 로 옮겨야 한다.** 자동 적용하지 않는다.

### B.4 산출근거 패키지

`reports/2026-1분기/`, `reports/2026-상반기/` 에 생성했다.

| 파일 | 내용 |
|---|---|
| `00_SUMMARY.md` | 산출 요약, 기준선별 점수, 교차검증 상태 |
| `01_raw_bars.csv` | 평가에 사용된 일별 원자료 전량 |
| `02_vwap.csv` | 종목·윈도우별 VWAP 중간값 |
| `03_trace.csv` | 증감율 → Peer → x → 평가주가 → 점수 전 단계 |
| `04_cross_check.csv` | KIS 월봉 대조 결과 |

검증하지 못한 달은 요약에 **명시적으로 표기**된다. 검증할 수 없는 것을
검증했다고 하지 않는 편이 낫다.
